import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tvvuetestapp.com',
  appName: 'tvvuetestapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
